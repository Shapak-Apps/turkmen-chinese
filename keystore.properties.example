# Release signing for local AAB/APK builds.
# Copy to keystore.properties (git-ignored) and fill in real values.
# Use forward slashes in the path — backslashes need escaping in .properties files.
storeFile=D:/path/to/your-keystore.jks
keyAlias=your-key-alias
storePassword=
keyPassword=
